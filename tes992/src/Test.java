/*
import java.awt.print.Pageable;
import java.util.*;
import java.util.function.IntUnaryOperator;
import java.util.stream.Collectors;

public class Test {
    public static void main(String[] args) {
        int arr[] = {1,2,3,2,4};
        boolean ans = findPalindrom(arr);
        //System.out.println(ans);
        int arr1[] = {2,3,5,4,7,5,8,7};
       */
/*List<Integer> list = (List<Integer>) Arrays.stream(arr).sorted(Comparator.reverseOrder(),Collectors.toList());
        System.out.println(list.get(list.size()-1));
        System.out.println(list.get(list.size()-2));
        System.out.println(list.get(list.size()-3));*//*



        int pageNum = 0;
        int pageSize = 5;

         private jpRepository jpa;
         @Query("select name from emplloyee e wheere e.id =:id" , native=true);
         public String getNameById(int id);

         @Query("select from Employee");
         publid List<Employee> getEmp():

        Pageable page = jpaREp.of(pageNum , pageSize);





         git init;
         git status;
         git pull;
         git push;
         git checkout;
         git remote origin master;
         git commite -m "";
         git confige user.name "";
         git config user.email " ;

            //select * from employee where like "A%" and "%A:;



    }

    private static boolean findPalindrom(int[] arr) {
        int i = 0;
        int j = arr.length-1;
        while (i<arr.length/2){
            if(arr[i]!=arr[j]){
                return false;
            }
            i++;
            j--;
        }
        return true;
    }
}
*/

package com.customform.repo;

import com.customform.model.Roll;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RollRepository extends JpaRepository<Roll , Integer> {
}
